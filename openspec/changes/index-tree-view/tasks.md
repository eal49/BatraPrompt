# Tasks

## 1. index.html
- [x] 1.1 Wrap each user section in `<details class="landing__group" open>` with `<summary class="landing__group-title">`
- [x] 1.2 Replace the `.landing__group-files` div with `.landing__tree` containing `.landing__row` links
- [x] 1.3 Strip `.html` from each link's display text (keep full filename in href)
- [x] 1.4 Add file count to the summary header

## 2. styles.css
- [x] 2.1 Style `summary.landing__group-title` with chevron that rotates on `details[open]`
- [x] 2.2 Style `.landing__tree` with left border line
- [x] 2.3 Style `.landing__row` as full-width compact link with right arrow and hover state
- [x] 2.4 Remove `.landing__group-files` and `.landing__item` card styles
