# Tasks

## 1. script.js — standalone template
- [x] 1.1 Add QR button to the `.ui` control bar in the `generateStandalone()` template
- [x] 1.2 Add the hidden modal overlay HTML to the template
- [x] 1.3 Add the JS to open the modal (set QR image src + show), close on backdrop click, close on Escape
